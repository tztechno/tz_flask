
```

flask_app/
│
├── app.py
├── sqlite-sakila.db  # SQLiteデータベースファイル
├── templates/
│   └── index.html
└── static/
    └── style.css


sample SQL

SELECT sqlite_version();

SELECT name FROM sqlite_master WHERE type='table';

```
